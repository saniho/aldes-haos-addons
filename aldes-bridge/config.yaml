---
name: Aldes Bridge
version: "0.6.2"
slug: aldes_bridge
description: Bridge MQTT/TLS pour Aldes T.ONE AquaAir — intercepte et contrôle la PAC
url: https://github.com/saniho/aldes-bridge
maintainer: saniho
codenotary: saniho@users.noreply.github.com
arch:
  - amd64
  - aarch64
  - armv7
init: false
host_network: true
ingress: true
ingress_port: 8080
panel_icon: mdi:thermometer
panel_title: Aldes Bridge
privileged:
  - NET_ADMIN
ports:
  "18883/tcp": 18883
map:
  - config:rw
options:
  mqtt_port: 18883
  box_ip: ""
  mode: bridge
  profile: tone-aquaair
  history_days: 90
  box_tz: Europe/Paris
schema:
  mqtt_port: int
  box_ip: str
  mode: list(bridge|proxy)
  profile: str
  history_days: int
  box_tz: str
