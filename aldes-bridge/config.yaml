---
name: Aldes Bridge
version: "0.12.5"
slug: aldes-bridge
description: Bridge MQTT/TLS pour Aldes T.ONE AquaAir — auto-discovery Home Assistant
url: https://github.com/saniho/aldes-bridge
maintainer: saniho
codenotary: saniho@users.noreply.github.com
arch:
  - amd64
  - aarch64
  - armv7
init: false
host_network: true
hassio_role: "manager"
ingress: true
ingress_port: 8080
panel_icon: mdi:heat-pump
panel_title: Aldes Bridge
privileged:
  - NET_ADMIN
ports:
  "18883/tcp": 18883
map:
  - config:rw
options:
  mqtt_port: 18883
  box_ip: ""
  mode: bridge
  profile: tone-aquaair
  history_days: 90
  box_tz: Europe/Paris
  ha_mqtt_enabled: true
  ha_mqtt_dry_run: true
  ha_mqtt_host: ""
  ha_mqtt_port: 1883
  ha_mqtt_user: ""
  ha_mqtt_password: ""
schema:
  mqtt_port: int
  box_ip: str
  mode: list(bridge|proxy)
  profile: str
  history_days: int
  box_tz: str
  ha_mqtt_enabled: bool
  ha_mqtt_dry_run: bool
  ha_mqtt_host: str
  ha_mqtt_port: int
  ha_mqtt_user: str
  ha_mqtt_password: password
