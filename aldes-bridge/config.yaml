---
name: Aldes Bridge
version: "0.4.0"
slug: aldes_bridge
description: Bridge MQTT/TLS pour Aldes T.ONE AquaAir — intercepte et contrôle la PAC
url: https://github.com/saniho/aldes-bridge
codenotary: saniho@users.noreply.github.com
arch:
  - amd64
  - aarch64
  - armv7
init: false
host_network: true
panel: true
panel_icon: mdi:thermometer
panel_title: Aldes Bridge
ingress: true
ingress_port: 8080
privileged:
  - NET_ADMIN
ports:
  "18883/tcp": 18883
  "8080/tcp": 8080
map:
  - config:rw
options:
  mqtt_port: 18883
  box_ip: ""
  mode: bridge
  profile: tone-aquaair
  history_days: 90
  box_tz: Europe/Paris
schema:
  mqtt_port: int
  box_ip: str
  mode: list(bridge|proxy|listen|raw)
  profile: str
  history_days: int
  box_tz: str
